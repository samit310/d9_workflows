{#
/**
 * @file
 * Default theme implementation to present an anonymous submission entity.
 *
 * This template is used when viewing a registered anonymous submission's page,
 * e.g., /admin/content/anonymous-submission)/123. 123 being the anonymous submission's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_anonymous_submission()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
